## 1er paso: 

    Crear un branch
    Incoporar el archivo csv desordenado
    Crear un script python con el algoritmo de ordenacion, que lea el archivo desordenado y grabe un nuevo archivo desordenado. 
    Una vez testeado que funciona, hacer merge a la rama principal
    Como resultado de ese merge, deberian quedar 4 archivos: 2 csv y 2 python. 



## 2do paso: 

    Crear un nuevo branch
    Agregar el algoritmo de ordenamiento como funcion en el python original (algoritmo de burbuja desarrollado, no usar funcioens sort propias del lenguaje). 
    Agregar una funcionalidad de menu, es decir, cuando se ejecute, solicite al usuario las opciones: 
        Indique el path del csv: ...
        El archivo esta ordenado: Y/N
    En caso que el archivo no esta ordenado, aplicar la funcion de ordenamiento (que grabaria un nuevo archivo temporal y luego comenzar la ejecucion desde ese archivo). 
    En caso que el usuario indique que el archivo esta ordenado: iniciar la ejecucion directamente. 
    Testear que funcione
    Mergear a la rama principal. 